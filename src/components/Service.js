import React from 'react';
import {Link} from 'react-router-dom';

export default function Service({service}) {
    const{}    
    return (
        <div>
            hello from service
        </div>
    )
}
